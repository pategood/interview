sort排序问题

  默认升序,排序只按首位数字来排

  原理: 将排序内容转化为字符串,通过对应的unicode表去进行对比
        由于unicode只定义了数字0-9,没定义二位数以上,所以排二位数以上的数字有问题
  
  解决办法:
    给sort定义一个比较函数组扁平化
   
    升序
    sort(function(x,y){
      return x-y
    })

    如果返回值小于0,则x移到y前   
