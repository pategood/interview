会改变原数组的:  
  push      向数组的末尾添加一个或多个元素,并且返回新的数组长度
  pop       删除数组的最后一个元素,并且返回新的长度
  shift     删除并返回数组的第一个元素
  unshift   向数组的开头添加 一个或多个元素 并且返回新的长度
  reverse   反转数组内元素的顺序
  sort      对数组的元素进行排序
  splice    用于插入、删除、替换数组的元素

不会改变原数组的:
  flat      数组扁平化,参数为拉平层数,默认为1,返回新数组
  concat    连接两个或更多的数组,并且返回新数组
  every     检测数组元素的每一个元素是否满足条件 
  some      检测数组元素中是否有符合指定条件的元素,返回布尔值      
  find      检测数组元素中是否有符合指定条件的元素,返回第一个元素
  toString  把数组转换为字符串,并返回结果 (字符串)
  join      把数组的所有元素放入一个字符
  filter    检测数组元素,并返回符合条件的元素数组                  (es6)
  map       通过指定函数处理数组的每一个元素,并返回处理后的数组     (es6)
  slice     截取数组的一部分,并返回一个新的数组
  from      接收类数组或可迭代对象 转换为数组                      (es6)




