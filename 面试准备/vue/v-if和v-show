
v-show 与 v-if 有什么区别？
  v-if切换时会销毁dom或重建dom  事件监听器和子组件 
    同时初始为false时,则什么也不做,直到条件第一次变为真时，才会开始渲染条件块。

  v-show 设置DOM元素的display样式，block为显示，none为隐藏，
    
应用:
  v-if 适用于在运行时很少改变条件，不需要频繁切换条件的场景；
  v-show 则适用于需要非常频繁切换条件的场景。