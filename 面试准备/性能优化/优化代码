减少DOM操作:

  一. 事件代理(事件委托)
    事件委托：利用冒泡的特性,将本应绑定在子元素的事件绑定到父元素
    这样点击子元素时发现其本身没有响应事件就冒泡到父元素上寻找事件作出响应
    
    这样与dom的操作就只需要交互一次

    优点:
      1.减少DOM操作，提高性能。
      2.随时可以添加子元素，添加的子元素会自动有相应的处理事件。

  事件代理的原理是DOM元素的事件冒泡。

    